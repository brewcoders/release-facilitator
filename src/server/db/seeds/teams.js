exports.seed = (knex, Promise) => {
    /*return knex('teams').del()
        .then(() => {
            return Promise.join(
                knex('teams').insert({
                    name:'Cards',
                    deployableid:7
                }),
                knex('teams').insert({
                    name:'Config',
                    deployableid:7
                })
            );
        });*/
};