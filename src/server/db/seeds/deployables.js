exports.seed = (knex, Promise) => {
    /*return knex('deployables').del()
        .then(() => {
            return Promise.join(
                knex('deployables').insert({
                    name: 'Hydra',
                    bambookey: 'DCW-HGH'
                }),
                knex('deployables').insert({
                    name: 'Tetra',
                    bambookey: 'DCW-TET'
                })
            );
        });*/
};